Act as the materials-planning engineer picking up after a failed ERP rollout. It truncated the authoritative inventory positions at `/app/data/inventory_positions.json` and left the requirements planner at `/app/workflow/plan_requirements.go` running against draft rules the planning board later reversed. Nothing the planner emits can be trusted until those positions are rebuilt. A pre-rollout snapshot and a journal of the movements posted after it survive beside the truncated file under `/app/data`. How the two combine, what a retraction does, which fields survive and the order of the result are governance decisions, recorded like every other rule here in `/app/incident/planning_governance_log.md`, and the positions must be restored at their expected path first. That replay is itself a program and has to stay one: leave it at `/app/workflow/recover_positions.go`, a single `package main` on the standard library, reading `--snapshot`, `--journal` and `--out` and defaulting them to the pre-rollout snapshot, the movement journal and the positions file under `/app/data`, so the same replay can be run again over a different snapshot and journal.

Then restore the planner where it stands, at `/app/workflow/plan_requirements.go`: a Go program built with the toolchain installed here and staying a single `package main`. Preserve its `--input` and `--output-dir` options and their defaults; the item master, bill of materials, independent demand, planning calendar, planning policy and work-centre capacity are always read from their fixed absolute paths under `/app/data`, and `--input` selects the positions file only. `/app/docs/report_spec.json` is the output contract: paths, schemas, required-field lists, container shapes and sort orders. It says nothing about how any value is derived. Reconstruct that from `/app/incident/planning_governance_log.md`, mostly routine minutes, where the later dated decision governs. A run writes exactly `/app/output/summary.json`, `/app/output/item_plan.json` and `/app/output/exception_queue.jsonl` inside the contract's 90-second budget. Derive every value from the operational inputs: standard library only, no planning or solver library, correct against an alternate positions file, identical across reruns, and leave the frozen rollout snapshot `/app/workflow/.plan_requirements.original.go` exactly as it is. Everything else under `/app/data`, the output contract and the minute book are inputs: the replay is what rewrites the positions file and the planner rewrites nothing under `/app/data` at all, so the snapshot, the journal, the item master, the bill of materials, the demand, the calendar, the policy, the work-centre capacity and `/app/docs/report_spec.json` all come back byte-identical.
